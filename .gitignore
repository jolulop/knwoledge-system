.env
__pycache__/
*.pyc
.venv/
node_modules/
.DS_Store
Thumbs.db
backups/*.zip
# Windows mark-of-the-web metadata (WSL)
*:Zone.Identifier
# Distributable docs archive (docs are tracked under docs/)
knowledge-system-docs-*.zip
indexes/vector/qdrant/
# Runtime job state (regenerated by intake scans); see ADR-0008.
db/jobs.sqlite
# Generated retrieval indexes are local runtime state, not committed: the
# keyword (FTS) index and the chunk index are rebuilt from wiki/normalized
# content by scripts/reindex_keyword.py and scripts/reindex_vector.py.
db/metadata.sqlite
normalized/chunks/chunks.jsonl
# Inbox is transient staging for scans; keep its contents out of git.
raw/inbox/*
!raw/inbox/.gitkeep
# Manifests are local runtime state: authoritative on disk, not committed.
raw/manifests/*.json
!raw/manifests/.gitkeep
# Uncomment if these raw files should also be kept out of git.
# raw/permanent/*
# raw/ephemeral/*
