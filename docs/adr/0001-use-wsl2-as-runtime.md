# Use WSL2 as the primary runtime

We will use Windows 11 as the host UI environment and WSL2 Ubuntu as the system runtime. This gives us better Linux tooling, Docker behavior, Python package compatibility, and agent CLI workflows while preserving Windows access to VS Code, Obsidian, and the browser.
