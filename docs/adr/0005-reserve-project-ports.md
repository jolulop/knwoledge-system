# Reserve dedicated local development ports

The Knowledge System will avoid common development ports already used by other projects. The backend API will use port 18000, the future MCP endpoint will use 18001, and the browser UI will use 13000.
