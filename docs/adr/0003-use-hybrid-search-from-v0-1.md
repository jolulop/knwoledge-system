# Use hybrid search from v0.1

The initial corpus is approximately 600 documents, so the system will not rely only on Obsidian-style index navigation. v0.1 will support keyword search, semantic/vector search, graph traversal, and summary-first wiki navigation.
