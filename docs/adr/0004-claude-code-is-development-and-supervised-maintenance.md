# Claude Code is for development and supervised maintenance

Claude Code will be used as the primary development assistant and for supervised repository maintenance workflows. Scheduled autonomous document processing should eventually run through API-based backend workers rather than depending on an interactive Claude Code login.
